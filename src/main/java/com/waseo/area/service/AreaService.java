package com.waseo.area.service;

import com.waseo.area.bean.Area;
import com.waseo.area.mapper.AreaMapper;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;

import java.io.Serializable;
import java.util.List;

public class AreaService{

    /*@Autowired
    private AreaMapper areaMapper;

    public Area get(String areaId) {
        return this.areaMapper.get(areaId);
    }

    public List<Area> list() {
        return this.areaMapper.list();
    }

    public Area create(Area area) {
        return this.areaMapper.create(area);
    }

    public Area update(Area area) {
        return this.areaMapper.update(area);
    }

    public void delete(String areaId) {
        this.areaMapper.delete(areaId);
    }*/
}
